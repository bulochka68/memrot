# Аудитор (mcp_audit) работает на стандартной библиотеке Python 3.11+.
# Ниже — только опциональные зависимости отдельных режимов.

# deployment-адаптер: чтение docker-compose.yml (без PyYAML читается только JSON-снимок)
PyYAML>=6.0

# тесты
pytest>=8.0.0
